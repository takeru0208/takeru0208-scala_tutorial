object PartialSatuation extends App {
    val a = Seq(3, 6, -5, 7)
    val n = a.length
    val k = 8

    
    // def isMatchAndResult(index: Int, partial: Seq[Int]): (Boolean, Seq[Int]) = {
    //     if (index == n){
    //         if (partial.sum == k)  {
    //             return (true, partial)
    //         } else{
    //             return (false, Seq())
    //         }
    //     } else {
    //         return (false, Seq())
    //     }
    // }
    // val (isMatch, result) = isMatchAndResult(n, Seq(3, 6, -5, 7))

    // val (isMatch, result) = (false, Seq())

    // def check(partial: Seq[Int]): (Boolean, Seq[Int]) = {
    //     return if(partial.sum = k) (true, partial) else (false, Seq())
    // }
    // (isMatch, result) = check(a)
    

    // if (isMatch) println(s"Yes ${result}") else println("No")
}
